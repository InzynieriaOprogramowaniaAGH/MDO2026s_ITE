apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-custom
  labels:
    app: nginx-custom
  annotations:
    kubernetes.io/change-cause: "dobra"
spec:
  replicas: 6
  selector:
    matchLabels:
      app: nginx-custom
  template:
    metadata:
      labels:
        app: nginx-custom
    spec:
      containers:
        - name: nginx-custom
          image: evernever2000/nginx-custom:2.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80