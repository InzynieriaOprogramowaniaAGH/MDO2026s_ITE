# Generated by Anaconda 42.27.12
# Generated by pykickstart v3.62
#version=DEVEL

# Wyłączenie instalatora graficznego i restart
text
reboot

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-42&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f42&arch=x86_64

# Run the Setup Agent on first boot
firstboot --disable

# Konfiguracja sieci, włączenie karty i ustawienie własnego hostname
network --bootproto=dhcp --device=link --activate --hostname=fedora-node-auto

# Sekcja czyszczenia dysku i partycjonowania
ignoredisk --only-use=sda

clearpart --all --initlabel --drives=sda
autopart --type=lvm

# System timezone
timezone Europe/Warsaw --utc

# Włączenie Dockera
services --enabled=docker

# Root password 
rootpw --iscrypted --allow-ssh $y$j9T$iVRwccMH1LSTj8.u6ONywq9.$sR8JA2e08xaDOASF1mseNAWi81f9JOaY/XFh4ZCOZp1
user --groups=wheel --name=fedora --password=$y$j9T$OG0wZwD1TpcXdC2DfpgNfNw2$GGm54CaF9cf8h56mMl.vBtBnVTFGlvrZ.vw.QjcmHLC --iscrypted --gecos="Amelia"

%packages
@^server-product-environment
moby-engine
nodejs
curl
%end

%post --log=/root/kickstart-post.log

cat << 'EOF' > /usr/local/bin/run-fastify-app.sh
#!/bin/bash
sleep 5

npm install -g zajecia6ak123

cd /usr/lib/node_modules/zajecia6ak123/ || cd /usr/local/lib/node_modules/zajecia6ak123/
npm start -- --port 3000 &

EOF

chmod +x /usr/local/bin/run-fastify-app.sh

cat << 'EOF' > /etc/systemd/system/fastify-app.service
[Unit]
Description=Automatyczne uruchomienie aplikacji Fastify z NPM
After=network.target

[Service]
Type=forking
ExecStart=/usr/local/bin/run-fastify-app.sh
Restart=always

[Install]
WantedBy=multi-user.target
EOF

systemctl enable fastify-app.service

%end