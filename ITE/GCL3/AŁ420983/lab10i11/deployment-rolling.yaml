apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-rolling
  labels:
    app: flask-app
spec:
  replicas: 4
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: "25%"
  selector:
    matchLabels:
      app: flask-app-rolling
  template:
    metadata:
      labels:
        app: flask-app-rolling
    spec:
      containers:
      - name: flask-container
        image: moja-aplikacja:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 5000
