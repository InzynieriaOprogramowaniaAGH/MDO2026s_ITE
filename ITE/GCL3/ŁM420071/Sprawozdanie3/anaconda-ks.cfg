# Generated by Anaconda 43.44
text
reboot
# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

cdrom
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-43&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f43&arch=x86_64
# Network information
network  --bootproto=dhcp --device=enp0s3 --ipv6=auto --activate
network  --hostname=fedora1

%packages
@^custom-environment
@guest-agents
@networkmanager-submodules
@standard
nodejs
npm

%end

# System authorization information
authselect enable-feature with-fingerprint

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.12.1
ignoredisk --only-use=sda
autopart
# Partition clearing information
clearpart --all --initlabel

# System timezone
timezone Europe/Warsaw --utc

#Root password
rootpw --plaintext Komputer123!
user --groups=wheel --name=fedora --password=$y$j9T$86HwusPu6hEpg3O1OyWgl/WD$695qr6gW61G3pl/jYkSQ.egL0hmGAa1FK4my653YME9 --iscrypted --gecos="fedora"



%post --log=/root/kickstart-post.log

npm install -g @freizen77/express-mdo-test

cat << 'EOF' > /usr/local/lib/node_modules/@freizen77/express-mdo-test/server.js
const express = require('./index.js');
const app = express();
const port = 3000;

app.get('/', (req, res) => {
    res.send('<h1>Sukces! Serwer Express.js dziala i odpowiada!</h1>');
});

app.listen(port, () => {
    console.log(`Serwer uruchomiony na porcie ${port}`);
});
EOF

cat << 'EOF' > /usr/local/bin/express-app
#!/bin/bash
cd /usr/local/lib/node_modules/@freizen77/express-mdo-test && node server.js
EOF

chmod +x /usr/local/bin/express-app

cat << 'EOF' > /etc/systemd/system/express-app.service
[Unit]
Description=Aplikacja Serwera Express.js z npm
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/express-app
Restart=always

[Install]
WantedBy=multi-user.target
EOF

systemctl enable express-app.service

%end