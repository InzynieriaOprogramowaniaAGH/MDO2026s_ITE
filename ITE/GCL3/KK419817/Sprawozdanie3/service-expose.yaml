apiVersion: v1
kind: Service
metadata:
  name: express-service-yaml
spec:
  type: NodePort
  selector:
    app: express-app-expose
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001
