pipeline {
    agent any
    stages {
        steps('Collect') {
            steps {
                checkout scm
            }
        }
        steps('Build') {
            sh 'docker build -t json-java-build:latest -f Dockerfile.builder .'
        }
        steps('Test'){

        }
    }
}